# 更新日志

## 朋友局 v2 可玩基线 — 2026-08-09

- 将对局、训练与回顾所用牌面替换为 Adrian Kennard CC0 Super Index
  高清资源，按目标尺寸离线栅格化，发行运行时不新增 SVG 依赖；
- 牌桌新增“当前牌型”提示；从对子起，以淡黄色边框标出最佳五张中
  实际构成牌型的核心牌，高牌只显示文字且不高亮踢脚；
- 牌型显示只读取动画中已经落定的完整街道，弃牌后隐藏，避免提前泄露公共牌。
- 数据统计页新增“清除当前记录”：首次点击只显示当前运行版本的确认框，确认后
  删除当前 `hands/history.jsonl` 并立即刷新统计/回顾；训练档案、模型、
  策略库与构建备份不受影响，文件占用或无权限时保留原记录。
- 修正 VPIP/PFR 把同一手内多次跟注、再加注重复计数的问题；两项指标现在
  都按“发生过该行为的手牌数 / 总手数”计算，每手每项至多累计一次。
- 开局人数扩展为 2–9 人并补齐 7/8/9 人位置；8/9 人桌在实际满 8 人开手时
  自动采用 UTG 目标 2BB live straddle，翻前由 UTG+1 首动；足额 straddle 时
  首次最小加注到 4BB，短码不足 2BB 时按实际全下且不虚增最小加注单位。
- AI 扩展为十五种动物身份与八类可选打法：新增棕熊、雄狮、猛虎、老龟、
  夜枭与黑豹，旧九名身份 ID/顺序保持不变；身份与打法可独立组合，收池、
  失池、成功诈唬和被诈唬后会触发各自的人格对白。
- 动物形象替换为十五张细腻的手绘酒馆肖像，同时保留动作状态、琥珀边框与
  程序化回退；运行资产压缩为 512×512，十五张合计约 5.42 MiB。
- 开局设置改为“酒馆选将”：先选 2–9 人目标，再从十五名候选中按入座顺序
  自由挑满 AI 阵容，并逐名配置八种既有打法和 10–1000BB 买入；重复身份、
  阵容未满或金额非法时均不能开始。
- 被移出的座位改为可点击“＋”：可选择未在桌上的 AI 身份、八类打法及
  10–1000BB 初始资金，预约下一手原子入座；爆仓配额框也可直接更换打法，
  好友加入模式保留同一席位契约但尚未联网。
- 非摊牌结束后新增 SHOW：人类可自愿亮牌，AI 成功诈唬时也可能亮牌；亮牌
  独立于下注状态机、写入历史事件并在下一手清空。
- 牌局信息新增独立“上一手结果”：显示手牌编号、摊牌/未摊牌、主池与各边池、
  实际赢家、摊牌牌型和逐人所得；历史记录同步保存结构化逐池派彩，平分与
  奇数筹码均按引擎真实结果记录，未摊牌不会泄露暗牌牌型。
- 全下结算改为“跑完公共牌 → 翻开底牌 → 宣布逐池结果 → 推送赢家筹码 →
  处理归零座位”；爆仓面板不再抢先遮住牌面，等待期间也不能绕过进入下一手。
- 每个在座角色桌前新增按真实码量拆分的多面值筹码堆；下注筹码会从玩家桌前
  飞到下注位、街道结束汇入中央底池，结算时再按各池实际金额推向获奖者。
- 推进朋友联机 Alpha 到首个后端会合点（现保留为 v1 历史基线）：新增严格 JSON v1、无歧义房间码、
  高熵恢复令牌、逐座位安全状态投影、RoomCore、单 mailbox actor、单活跃房间
  注册表和仅监听 localhost 的 WebSocket 服务；创建/加入、ready/start、权威
  CALL/ALL-IN、SHOW、下一手、爆仓补入/离席、UUID 幂等、版本冲突、限流与恢复
  替换均有回归，双 WebSocket 协议客户端自动化已完成弃牌局和全下摊牌局。
  底层与安全投影统一支持 2–9 真人，当前实际可玩验收先以 HU 为基线。
- W3a 增加安全启动器与临时公网会合边界：默认仍只监听 localhost 和 `/ws`；
  Quick Tunnel 模式每次生成精确私密 WebSocket 路径，在本机/公网 health 与
  hello/ping 全部通过后才显示完整 WSS 地址，并在 Ctrl+C 时回收其子进程。
  `/health` 保持无状态公开，空房默认保留 900 秒供 token 恢复；私密路径明确
  不作为正式认证，Quick Tunnel 无 SLA，URL 泄露须重启。
- 真机最小会合新增单窗口入口 `run_friends_phone_test.cmd`：自动创建两人连接
  验证房，直接显示 WSS 与房间码，并以经过房间/视角/座位结构校验的状态确认
  第二位玩家加入；恢复 token 不打印、不落盘。旧 CLI 流程保留作协议诊断，
  该自动房只做连接验证，不与可玩房主共用。
- Windows 源码端新增后台 WSS 客户端、房主大厅和服务器权威牌桌：主菜单显式
  区分单机/朋友联机，专用 `run_friends_desktop_public.cmd` 自动创建两人房；手机
  可凭脱敏大厅中显式复制的完整 WSS、6 位房间码和昵称加入。支持准备/开局、
  合法下注、SHOW、下一手、补码/离席及断线恢复；关闭窗口会联动回收本轮服务。
- Android v0.5.1 历史基线在保留离线单机的同时，接入了 2–9 真人建房、个人视角
  移动图形牌桌、服务端额度范围滑杆和前后台横屏恢复；冻结源码为 `8650fce…`，
  APK SHA-256 为
  `53723b73d8ee411bdea1a2c7a14070c5c465f1ccc5355ad76708a9650263a20f`。
  旧版单真机链路反馈不能外推为本精确 APK 的双 Android 完整整手/恢复验收。
- JSON v1 新增过渡期最小服务器 AI 契约：`room.ai.fill`/`room.ai.clear` 只接受空 body，
  仅房主可在 LOBBY 补满全部空席或只清 AI；`room.state` 通过
  `features.server_ai_fill`、`occupant_type`、`persona_id/style_key` 区分席位。
  AI 无 token/WS，只看自己的 perspective，由单 mailbox actor 每步独立推进版本
  与广播，并在两手间按房间 buyin 自动补码；不会自动下一手或 SHOW。Android
  v0.5.1 的 AI 按钮未接线，且 v1 客户端不能连接当前 v2 服务。旧 v1.0.0 EXE
  尚未重新打包联机代码。
- 朋友局升级为破坏性 JSON v2：恢复 token 改绑稳定 `member_id`，不再绑定 seat；
  创建者和加入者均先作为未入座成员，2–9 个物理座位初始显示“＋”，真人取消准备后
  可自由选座/换座，进行中空位可预约下一手。房主也须选座准备，至少两名真人/AI
  即可开局，不再假定房主固定 seat 0 或房间必须坐满。
- v2 把服务器 AI 扩展为指定座位的 `room.ai.add/remove/rebuy/style`：房主为每席选择
  十五种唯一动物身份、八种打法和 10–1000BB 买入；AI 仍无 token/WS，只读自身
  perspective，并以默认 0.8–1.2 秒间隔逐步行动。旧 fill/clear 仅保留为大厅便利命令。
- v2 新增目标栈补码、低码提示、审批和暂停：真人低于 100BB 须申请或跳过提示；
  当前码量不高于 400BB（或房主本人）的申请直接批准，更深筹码由房主审批，所有
  补码在下一手生效。未决爆仓/提示/审批会阻塞自动续手；普通结算停留 3 秒后自动
  发下一手。暂停会取消计时并冻结所有牌局变更，只保留公共历史与码量查看。
- Windows v2 联机牌桌按单机体验补齐圆桌选座、牌型提示/成牌高亮、行动气泡、
  ChipFly/muck/SHOW、加注滑杆、爆仓与补码面板、暂停卡片及安全公共牌局摘要。
  连续版本按各自冻结安全 state 排队；预约席当前手保持公共旁观视角；全下终局按
  跑牌→赢家说明→派彩→处置顺序展示。连接替换后在 lifecycle lock 内二次验明身份，
  旧连接不能再抢入命令。Windows 全量回归 635 项，8 张联机矩阵和原 19 张单机截图
  已逐张验收；本轮未重打包 EXE。
  Android v0.6.0 已在独立 WSL 工作树按冻结协议完成增量接线并保留离线单机；
  arm64-v8a APK 为 30,121,709 bytes，冻结源码
  `b4cba9d392f9a55f88611635b8056af1f1855c3e`，SHA-256 为
  `158bc4f32288122fcded2c131e0cfc2829d947ccea08d7f62c1c2d1c65490411`。
  用户已完成当前 Windows v2 权威服务配合两台 Android 的人工可玩性验收；这不
  外推为尚未逐项记录的完整 2/6/9 人布局、生命周期与恢复矩阵。APK 使用内部
  测试签名，只供朋友间 sideload；公开仓库不分发 APK 或 Android/WSL 构建源码。

## v1.0.0 — 2026-07-28

《酒馆德州》的首个稳定 Windows 桌面版。

### 主要功能

- 2–6 人单机无限注德州，六席可分别设置 10–1000 BB 初始买入；
- 牌手清空后可选择重新配额或移出对局；
- 完整的发牌、下注、弃牌入堆、摊牌、行动来源和反馈动效；
- 五名风格化动物 AI；翻后恰剩两名竞争者时实验性使用
  1,244,484 手 HU NFSP 轻量模型；
- GTO 辅助、翻前图表、HU TexasSolver 训练场、Drills、回顾和统计；
- Windows one-folder 便携发行，自动保护已有存档并生成发布校验信息。

### v1.0.0 已知限制

- NFSP 观测不含位置、街道和行动历史，只是 HU 翻后粗近似；翻前及三人以上
  仍使用人格启发式 AI，不能称为多人 GTO。
- TexasSolver 只支持 HU、只使用 CPU；复杂树可能耗时数分钟，v0.2.0 导出
  不包含 EV。
- 游戏当前只启用两个低精度、精确牌面匹配的预计算策略 spot；15 个生产
  spot 仍在隔离目录中，未纳入本版。
- 面对翻前加注的辅助建议仍为启发式近似。
- Windows x64 便携版没有安装器、自动更新或数字签名；首次运行可能显示
  “未知发布者”。
- 可写数据保存在 EXE 同级目录，必须把整个发行目录解压到有写权限的位置。
- Android APK 是独立可行性支线，不属于桌面 v1.0.0。
